{
  "name": "enqflow-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix web && npm install --prefix desktop",
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix web\" \"npm run dev --prefix desktop\""
  }
}
